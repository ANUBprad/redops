"""Dependency Injection container for the RedOps Eval platform.

The DI container manages service creation and lifetime.
All shared services must be registered in the container and
resolved through it rather than instantiated directly.
"""
