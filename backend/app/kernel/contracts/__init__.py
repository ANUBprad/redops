"""Immutable configuration contracts for the platform.

All service, provider, and plugin configurations are derived
from these base contracts. Configurations are immutable after
creation (frozen dataclasses / Pydantic models).
"""
