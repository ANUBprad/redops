"""Repository contracts for data access abstraction.

These interfaces define how domain entities are persisted and retrieved.
Implementations (SQLAlchemy, in-memory, test doubles) live in the
infrastructure layer and depend on these contracts.
"""
